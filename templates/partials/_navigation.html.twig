<nav>
    <ul style="display: flex; list-style: none; margin: 0; padding: 0; gap: 20px;">
        <li><a href="{{ path('home') }}" style="font-weight: 600;">Home</a></li>
        <li><a href="{{ path('services') }}" style="font-weight: 600;">Services</a></li>
        <li><a href="{{ path('clients') }}" style="font-weight: 600;">Clients</a></li>
        <li><a href="{{ path('cv') }}" style="font-weight: 600;">CV</a></li>
        <li><a href="{{ path('contact') }}" style="font-weight: 600;">Contact</a></li>
    </ul>
</nav>
